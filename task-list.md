# Task List

## Completed In This Pass

1. Enforce Agent 3 deployment throttling with the shared content deployment limiter.
2. Fix Agent 7 to write into the actual `performance_snapshots` schema.
3. Dispatch `optimization_actions` into `agent_tasks` with dedupe protection.
4. Strengthen Agent 3 quality/compliance checks and Hugo legal partials.
5. Draft `CallForge PRD v9.md` as an addendum in the same style as v8.

## Next

1. Replace static candidate cities with a database-backed deployment queue.
2. Add payout-aware scoring inputs to Agent 1.
3. Add cache source metadata for estimated vs official keyword data.
4. Replace mock performance data with a real provider.
5. Implement an indexation-based deployment kill switch.
